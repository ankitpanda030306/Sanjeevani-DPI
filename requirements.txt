scikit-learn>=1.3
numpy>=1.24

# Optional - enables the higher-quality embedding backend in
# retrieval_module.py. If not installed (or no network to download model
# weights), the pipeline automatically falls back to the built-in TF-IDF
# backend, so this is not a hard requirement.
sentence-transformers>=2.2

# Optional - swap in for schema_validation.py's hand-rolled validator.
pydantic>=2.0
